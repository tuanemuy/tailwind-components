import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Components/Introduction" />

# Components

Preline UIコンポーネントライブラリのドキュメントです。

## 構成

### Atoms

基本的なUIコンポーネントです。他のコンポーネントの構成要素として使用されます。

- Avatar: ユーザーアイコン (src, size, status)
- Badge: ラベル/ステータス (variant, size)
- Button: ボタン (variant, size, leftIcon, rightIcon)
- Checkbox: チェックボックス (checked, onChange)
- Icon: アイコン (name, size)
- Image: 画像 (src, alt, aspectRatio)
- Input: テキスト入力 (type, placeholder, error)
- Kbd: キーボードショートカット
- Label: フォームラベル (htmlFor, required)
- Link: リンク (href, variant, external)
- ProgressBar: 進捗バー (value, max, variant)
- Radio: ラジオボタン (checked, name, value)
- Separator: 区切り線 (orientation)
- Skeleton: ローディングプレースホルダー
- Spinner: ローディング (size)
- Switch: トグルスイッチ (checked, onChange)
- Textarea: 複数行入力 (rows, placeholder)
- Tooltip: ツールチップ (content, position)

### Molecules

Atomsを組み合わせた複合コンポーネントです。

- Accordion: 開閉パネル
- Alert: アラートバナー (variant, title)
- AvatarGroup: アバター群 (avatars, max)
- Breadcrumb: パンくずリスト (items)
- ButtonGroup: ボタングループ
- CheckboxGroup: チェックボックス群 (options, value)
- ColorPicker: カラー選択 (InlineColorPicker)
- ComboBox: 検索可能セレクト (options, searchable)
- DatePicker: 日付選択
- DateRangePicker: 日付範囲選択
- Dropdown: ドロップダウンメニュー
- EmptyState: 空状態表示 (NoData, NoFiles, NoResults, NoUsers)
- FileItem: ファイル項目 (name, type, size)
- FormField: ラベル+入力+エラー (label, error, required)
- IconButton: アイコンボタン (icon, variant)
- InlineSelect: インラインセレクト
- Menu: メニュー (MenuItem, SubMenu, ContextMenu)
- MultiSelect: 複数選択セレクト (options, value)
- Pagination: ページネーション (currentPage, totalPages)
- Popover: ポップオーバー
- RadioGroup: ラジオボタン群 (options, value)
- RangeSlider: スライダー (min, max, value)
- Rating: 星評価 (value, max)
- SearchInput: 検索入力 (onSearch, shortcut)
- SectionFilterBar: フィルターバー
- Select: セレクトボックス (options, value)
- SidebarSearch: サイドバー検索
- StatCard: 統計カード (label, value, icon, trend)
- Stepper: ステップインジケーター (steps, currentStep)
- Tabs: タブ (items, activeTab)
- TagInput: タグ入力 (tags, onAdd, onRemove)
- TimePicker: 時刻選択
- Toast: トースト通知 (variant)
- TrendIndicator: トレンド表示 (value, direction)

### Organisms

より複雑な機能を持つコンポーネント群です。

#### Layout
Header, Sidebar, Footer, Modal, Drawer, PageLayout, MultiColumnLayout

#### DataDisplay
Card, Table, DataTable, ImageGallery, MasonryGrid

#### Forms
Form, FilterForm, FileUpload, FeedbackForms, CheckoutForm

#### Overlays
ConfirmModal, InviteModal, ShareModal, SettingsModal

#### Cards
AddressCard, ContactCard, FileCard, ProjectCard

#### PageSections
ActivityFeed, Banner, Timeline, TitleBar

#### E-Commerce
CategoryCard, ProductCard, ProductDetails, ShoppingBag

#### DataVisualization
AnimatedStats, BrowsersCard, SalesStatsCard

#### InboxChatMessages
ChatLayout, ChatWidget, InboxLayout, MessageBubble

#### Finance
BalanceCard, TransactionList, PaymentCard

#### Tables
FilesTable, InvoicesTable, OrdersTable

#### DomainSpecific
CalendarScheduling, CommandPalette, KanbanBoard, MediaPlayer

#### Marketing
CareersSection, CTASection, FAQSection, HeroSection

### Pages

完全なページテンプレートです。

#### 認証
AuthPage, LoginCenteredPage, CreateAccountPage

#### オンボーディング
OnboardingRolePage, OnboardingProjectPage

#### ダッシュボード
DashboardPage, ProfilePage, SettingsPage

#### コンテンツ
ArticlePage, BlogListPage, BlogPostPage

#### E-Commerce
ProductPage, CheckoutPage, CheckOrderPage

#### その他
PricingPage, ContactPage, ErrorPage, NotFoundPage

## Icons

利用可能なアイコンについては Icons/Introduction を参照してください。
